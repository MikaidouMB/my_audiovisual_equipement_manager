{% extends 'base.html.twig' %}

{% block title %}Editer Materiel{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card">
                    <div class="card-header">
                        <h2 class="mb-0">Editer Materiel</h2>
                    </div>
                    <div class="card-body">
                        {{ include('materiel/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>
                <div class="mt-3 d-flex justify-content-between">
                    <a href="{{ path('admin_index') }}" class="btn btn-secondary">Retour à la liste</a>
                    <div>
                        {{ include('materiel/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
