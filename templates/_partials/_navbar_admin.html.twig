<!-- Inclusion de la bibliothèque Bootstrap Icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">

<!-- Navbar pour l'espace admin -->
<nav class="navbar navbar-expand-md navbar-dark bg-dark justify-content-center mb-4 border-bottom border-light">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="adminNavbar">
            <ul class="navbar-nav mx-auto justify-content-center">
                <li class="nav-item mr-3">
                    <a class="nav-link" href="{{ path('admin_index') }}">
                        <i class="bi bi-house-fill"></i> Accueil
                    </a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link" href="{{path('admin_app_devis_list')}}">
                        <i class="bi bi-file-earmark-text me-1"></i> Devis
                    </a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link" href="{{path('admin_app_user_index')}}">
                        <i class="bi bi-person-circle me-1"></i> Utilisateurs
                    </a>
                </li>
                <li class="nav-item mr-3">
                    <a class="nav-link" href="{{path('admin_shops')}}">
                        <i class="bi bi-shop me-1"></i> Boutiques
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin_app_evaluations_list')}}">
                        <i class="bi bi-card-checklist me-1"></i> Evaluations
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
