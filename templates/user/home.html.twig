{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <div class="container mb-5">
        <div class="row mb-5">
        {{ include('_partials/navbar_profile.html.twig') }}
    </div>

    <h1>Utilisateur</h1>

    <table class="table mb-5">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ user.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user.adresse }}</td>
        </tbody>
        
    </table>

<div class="d-flex align-items-center">
    
    <a href="{{ path('user_profile_edit', {'id': user.id}) }}" class="btn btn-primary" style="margin-right: 15px;">Éditer</a>
    
    <div>
        {{ include('user/_delete_form.html.twig') }}
    </div>
</div>
<hr>


        </div>
    </div>
{% endblock %}
